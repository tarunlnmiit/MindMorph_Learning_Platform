# MindMorph Learning Platform — QA / End-to-End Test Plan

**Audience:** QA / Testing team
**Scope:** Everything implemented to date — the multi-agent learning prototype and the adaptive
learning loop.
**Build under test:** `main` branch (latest). UI is the Next.js web app (`web/`) over the FastAPI backend (`api/`).
**Last updated:** 2026-09-10

---

## 1. What this product does (one-paragraph orientation)

MindMorph turns a learning goal (e.g. *"I want to learn Python for data science"*) into an
**interactive skill roadmap**. A team of LLM agents researches the goal, builds a **skill dependency
graph**, and lets the learner click any skill to get a **lesson** (teaching content + a practice
exercise). The learner's exercise is **graded live**, the score **updates mastery** on the graph, and
a low score **adapts the graph** — adding remedial prerequisite skills and regenerating the lesson to
target the learner's gaps. Two other routes exist (a standalone lesson, a standalone exercise) — the
Orchestrator answers those queries directly instead of building a path, and the dashboard says so
rather than navigating anywhere.

**A note on non-determinism:** all content, lessons, and exercises are **generated by an LLM at
runtime**. The same query will produce different wording, different exercises, and different unit
tests on each run. Test cases below therefore assert on **structure and behavior** (a graph appears,
a node turns green, a remedial node is added) — **not** on exact text. Where an exercise must be
solved, read the generated unit tests out of the session payload to see what is required (the lesson
panel shows the statement and the editor, not the tests), then use the recipes in **Appendix A**.

---

## 2. Scope

### In scope (implemented — test these)
| Area | Feature |
|---|---|
| **SCOUT** route | Skill dependency graph generation, reviewer verdict, specialist outputs (Academic / Market / Practical) |
| **Adaptive learning loop** | Skill graph as the node picker, composed lesson (content + exercise), lesson caching, live grading, mastery capture, graph adaptation (remedial nodes / unlock edges), score-aware regeneration, sticky mastery |
| **CONTENT** route | Standalone "quick lesson" (dual-path: creative + live web grounding) — answered directly, no path created |
| **EXERCISE** route | Standalone exercise — answered directly, no path created |
| **Persistence & grounding** | Postgres session store (JSONB rows, `DATABASE_URL`; `MINDMORPH_STORE=memory` is the zero-infra opt-out, not the default) + opt-in local RAG / vector search (`MINDMORPH_RAG=1`, `rag/`, pgvector) |
| Cross-cutting | Logging, error handling, LLM provider selection (keyless Ollama path, Groq key-pool rotation, fallback) |
| Automated tests | `pytest` suite |

### Out of scope (not implemented — do **not** raise bugs for these)
Real authentication (the email field on the login gate is MVP identity only — no password, no
session token, no account records), multi-**vendor** model routing (GPT / Claude / Gemini / Bedrock —
the two backends that do exist, Groq and Ollama, are in scope), Redis / S3 / Kafka, containerized
grading sandbox, real dataset ingestion (dataset exercises return links), deployment / CI / K8s,
telemetry and observability pipelines. See `docs/IMPLEMENTATION_STATUS.md` for the full roadmap.

---

## 3. Test environment & setup

### 3.1 Prerequisites
- macOS or Linux, **Anaconda/Miniconda** installed.
- A **`conda` env named `mindmorph`** (the project's required env).
- **[Ollama](https://ollama.com) installed and running** (`http://localhost:11434`) with both tier
  models pulled — `ollama pull qwen2.5:7b` and `ollama pull qwen2.5:14b`. This is the default backend
  and the only one that needs no account.
- API keys — **all optional**; there is no mandatory secret:
  - `GROQ_API_KEYS` — *optional*, **plural**, comma-separated **pool** of Groq keys. Present ⇒ hosted
    Groq (`openai/gpt-oss-120b`) runs in front of Ollama and is much faster. Absent/empty ⇒ everything
    runs on local Ollama; `config.py` raises nothing.
  - `APIFY_API_TOKEN` — *optional*; only the **Market** tab / Market agent needs it. Its absence is
    expected to degrade gracefully (no crash).
  - `GITHUB_TOKEN` — *optional*; improves the Practical agent's GitHub results.

> **Speed expectation:** on the keyless Ollama path inference is slow — a full skill-graph build takes
> **minutes** on `qwen2.5:14b`. Slowness on Ollama is **not** a bug; only raise a timeout defect when
> Groq keys are configured. Test Suites B–G on the Groq path if you have keys.

### 3.2 One-time setup
```bash
# from the project root
git checkout main && git pull
conda run -n mindmorph pip install -r requirements.txt   # if env not already provisioned
```
A `.env` file in the project root is **optional** — with no `.env` at all the app runs on Ollama. To
test the hosted path, create one:
```
GROQ_API_KEYS=<key1>,<key2>,<key3>   # optional pool; unset/empty = Ollama-only
APIFY_API_TOKEN=<optional key>
GITHUB_TOKEN=<optional token>
```

### 3.3 Launch the app
```bash
# Backend (FastAPI). Default store is Postgres — start it first: docker compose up -d db
conda run -n mindmorph uvicorn api.main:app --port 8000
# ...or run with no infra at all (sessions won't survive an API restart):
# MINDMORPH_STORE=memory conda run -n mindmorph uvicorn api.main:app --port 8000
# Frontend (Next.js), separate terminal
cd web && npm install && npm run dev
```
Open **http://localhost:3000** (API on :8000). `web/.env.local` must set
`NEXT_PUBLIC_API_URL=http://localhost:8000`. Backend runtime logs print to the uvicorn terminal and
`logs/mindmorph.log`. (The legacy Streamlit app was retired in P3 #12.)

### 3.4 Run the automated test suite (separate terminal)
```bash
conda run -n mindmorph python -m pytest -q
```

---

## 4. How the three "routes" work (important for testers)

Everything starts from the same **Start a new path** box on the dashboard. An Orchestrator agent reads
your query and routes it to one of three flows. Phrase the query to force the route you want to test:

| Route | Triggered by (intent) | Example queries |
|---|---|---|
| **SCOUT** (full roadmap + adaptive loop) | "I want to learn / teach me / roadmap for …" | `I want to learn Python for data science` · `Teach me machine learning` |
| **CONTENT** (one quick lesson) | "Explain / give me a lesson on / what is …" | `Explain Python list comprehensions` · `Give me a quick lesson on REST APIs` |
| **EXERCISE** (one standalone exercise) | "Give me an exercise / a coding challenge on …" | `Give me a coding exercise on Python loops` |

Only **SCOUT** produces a session to navigate to. CONTENT and EXERCISE return their answer on the
`/sessions/stream` response with no `session_id`, so the dashboard stays put and shows an **"Answered
directly"** notice instead (`web/app/page.tsx`). Their generated content is in the response payload,
not on screen — Suites D and E therefore read it from the API.

> Routing is LLM-driven. If a query lands on the wrong route, rephrase using the cues above. A
> mis-route on an ambiguous query is **low severity**, not a blocker.

---

## 5. Test suites

Severity guide: **Critical** (blocks core flow / data loss) · **High** (feature broken) ·
**Medium** (degraded UX) · **Low** (cosmetic).

### Suite A — Environment & smoke

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| A1 | Keyless launch runs on Ollama | Remove/empty `GROQ_API_KEYS` (delete `.env` or comment the line), Ollama running, launch, then run one **CONTENT** query (`Explain Python list comprehensions`) end to end | Backend starts with **no** key error; the lesson generates and renders (slowly — minutes is fine). Logs contain **no** `llm:` rotation/fallback lines, and Ollama shows the request (`ollama ps`, or Ollama's own server log). **Any startup failure or "key not found" error is a Critical defect** — the keyless path is a headline feature | Critical |
| A2 | Launch with keys | Set `GROQ_API_KEYS` to one or more valid keys, launch, run the same CONTENT query | http://localhost:3000 shows the login gate — headline **"Click a skill. Learn it. Prove it."** and one email field (`web/app/page.tsx`); after signing in, the query answers **noticeably faster** than A1; no `llm:` warnings in the logs | Critical |
| A5 | Key rotation on a bad key | Set `GROQ_API_KEYS=bad1,bad2,<valid key>` (invalid keys → HTTP 401, a rotate status), launch, run any query | Logs show `llm: key 1/3 failed with HTTP 401 … rotating to next key`, then key 2/3, then the answer returns from key 3. **UI result is normal** — rotation is invisible to the learner. With no valid key in the pool, logs instead show `llm: all N keys exhausted — falling back` and the answer still arrives (from Ollama) | High |
| A6 | Bad model id fails loudly, once | Set `MINDMORPH_GROQ_MODEL=openai/does-not-exist` with a valid `GROQ_API_KEYS` pool, launch, run any query | Logs show **one** `llm: key 1/N failed permanently with HTTP 404 — NOT rotating, falling back` at **ERROR** level; the other keys are **not** tried. The answer still returns via Ollama (slower). A 404 that rotates through the pool, or degrades with **no** ERROR line, is the defect | High |
| A3 | Automated suite | Run `pytest -q` | All tests pass (**232 collected** at time of writing); no failures or errors | High |
| A4 | Sign out / sign back in | With a path open, go **← All paths** → click **`<email>` · sign out** (dashboard header), then sign in again with the same email | Sign-out returns to the login gate with no crash; signing back in lists the same path under **Your paths** with **Resume →**, and resuming restores the graph and mastery. (There is no agent re-initialize control — persistence replaced it) | Medium |

### Suite B — SCOUT route (skill roadmap)

Precondition: app running, signed in, on the dashboard.

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| B1 | Generate roadmap | In **Start a new path** enter `I want to learn Python for data science` → **Generate** | The button reads **"Building…"** and a stage label streams beside a spinner (orchestrate → scout → specialists → consensus → reviewer), then the browser lands on `/session/<id>` with an interactive **React Flow skill graph** (`web/components/SkillGraph.tsx` — not a Mermaid render; Mermaid appears only inside lesson markdown) | Critical |
| B2 | Completion header | After B1 | The header card shows `0/N` above **skills complete** (N = node count) (`web/app/session/[sessionId]/page.tsx`) | High |
| B3 | Reviewer verdict | After B1, read `GET /sessions/<user>/<session>` | `learning_session.review_passed` (bool) and `review_notes` are populated. Note: no UI surfaces the verdict today — this is an API-payload check | Medium |
| B4 | Skill graph JSON | Same response as B3 | Valid JSON: `skill_graph.summary`, `nodes[]` (id/label/description/level), `edges[]` | Medium |
| B5 | Node cards | Inspect the graph | Each node is a card with a status glyph, its label (clamped to two lines), and a status word — **Available** on a fresh path. Locked nodes render dimmed and non-clickable | Low |
| B6 | Specialist outputs | Same response as B3 | `academic_output`, `market_output`, `practical_output` each carry content **or** are `null` — a null (e.g. Market with no `APIFY_API_TOKEN`) is graceful degradation, never a crash | Medium |
| B7 | Empty query guard | Clear the input → click **Generate** | Nothing happens / no crash (`handleCreate` returns early on empty input) | Low |

### Suite C — Adaptive learning loop (core feature)

Precondition: a roadmap is generated (Suite B).

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| C1 | Prerequisite layout | Inspect the graph after B1 | Nodes are laid out left-to-right by the prerequisite DAG (dagre, `web/lib/graphLayout.ts`) — a prerequisite always sits left of the skill it unlocks, so foundational work reads first. (The old level-sorted dropdown is gone; the graph itself is the picker) | Medium |
| C2 | Open lesson | Click an unlocked node card | **"Composing lesson…"** appears under the graph, then a **Lesson** panel renders below it: markdown teaching content (its own `<h1>`) and a **Practice** section with the exercise statement and an editor (`web/components/LessonPanel.tsx`) | Critical |
| C3 | Lesson caching | Open node X, click node Y, click back to X | X loads **instantly** (no "Composing lesson…") — served from cache; backend logs `service: lesson <id> HIT (cache_hits=N)` | High |
| C4 | Grading harness generated | For a coding exercise, read `lessons.<node_id>.exercise.grading_artifact` from `GET /sessions/<user>/<session>` | `format` is `coding_challenge` and `unit_tests[]` holds the generated `def test_…` functions. Note: the learner-facing panel deliberately shows only the statement and the editor — the tests are not on screen, so this is an API-payload check | High |
| C5 | Grade — CORRECT | Submit a fully-correct solution (Appendix A) → **Grade my submission** | Panel shows `100%` and `N/N tests passed` in green. If the node has **no unmastered prerequisites**, its card flips to **✓ Complete** and the `N/M` counter increments. If a prerequisite is still incomplete, it shows **🔒 Locked** and the counter does **not** increment — this is correct (see C14), not a bug | Critical |
| C6 | Adaptation on mastery | After C5 | An **unlock edge** is added from the mastered node to a downstream node (visible in the graph / JSON); **no** new node | Medium |
| C14 | Prerequisite-gated completion | Fail a node (→ spawns a remedial **prerequisite**), then **master that same node** while the new prerequisite is untouched | Node shows **🔒 Locked** (passed its own exercise but a prerequisite is incomplete), **not** ✓ Complete; the `N/M` count does not include it. Then master the prerequisite → the node flips to **✓ Complete** and the count increments | High |
| C15 | Locked lesson not openable | Click a node card rendered **dimmed** with the **🔒 Locked** status (a prerequisite isn't complete) | No lesson opens; a message **"🔒 Locked — first complete: \<prerequisite names\>"** appears under the graph. A node downstream of a locked node is also locked. The gate is enforced server-side too (`POST …/lessons/<id>` → 409) | High |
| C16 | Unlock on prerequisite completion | Master the prerequisite(s) named in C15, then click the previously-locked node | The card is no longer dimmed or Locked; the lesson opens normally | Medium |
| C7 | Grade — WRONG | On an **unmastered** node, submit a failing solution (Appendix A) | `0%` and `0/N tests passed` in red; the graph camera pans to the change; the node's raw `status` becomes `needs_review` but it **displays as 🔒 Locked** (a sub-40 grade sets `remediation_pending`, which locks it until the new prerequisites are cleared); a **new remedial node** animates in with an edge pointing **into** the failed node, and a **Graded** card replaces the lesson panel saying how many prerequisite skills were added | Critical |
| C8 | Score-aware regeneration | After C7, master the remedial prerequisite(s) so the failed node unlocks, then click it again | Lesson **regenerates** ("Composing lesson…", i.e. a cache MISS — the failing grade dropped the cached lesson) and the new content addresses the recorded `weaknesses`. Backend log: `ContentAgent: generating … (remediation=True)`. Do not assert on exact headings — the wording is LLM-generated | High |
| C9 | Open remedial node | Click the newly-added remedial node | A lesson for that prerequisite sub-skill renders | Medium |
| C10 | Grade — PARTIAL | On an **unmastered** node, submit a partially-correct solution (Appendix A), tuned to 40–79% | Mixed result (e.g. `3/5 — 60%`); node turns **◐ Keep practicing**; no remedial node, no lock | High |
| C11 | Sticky mastery | On a node already **✓ Complete**, submit a wrong solution | Node **stays ✓ Complete** (mastery is sticky); `best_score` unchanged in the payload; `attempts` increments | High |
| C12 | Stale panel cleared | Fail a node (C7), reopen it (new exercise generated) | The old grade result is **gone** — the freshly opened lesson does not show a result from the previous exercise | Medium |
| C13 | ID stability | Trigger several adaptations | Existing node ids are **never renamed/removed**; only new nodes/edges are added; previously-mastered nodes stay **✓ Complete** | High |

> **Status glyphs reference** (`web/lib/status.ts` `STATUS_STYLE`): **✓ Complete** · **🔒 Locked**
> (prerequisites pending, or a sub-40 grade's remediation lock) · **◐ Keep practicing** (40–79) ·
> **↻ Needs review** (< 40) · **○ Available**. A node earns **✓ Complete only when it AND all its
> prerequisites (transitively) are mastered**; a node that passed its own exercise (score ≥ 80) but
> still has an incomplete prerequisite shows **🔒**. Thresholds are Python-side and single-sourced in
> `services/mastery.py` (`MASTERY_THRESHOLD=80`, `REVIEW_THRESHOLD=40`, `REMEDIATION_THRESHOLD=40`).
> Per-exercise score→band on a 5-test exercise: 5/5 or 4/5 → mastered, 3/5 or 2/5 → ◐, ≤1/5 → sub-40
> (lock + remediation). The `N/M` header counts **fully-complete** nodes.

### Suite D — CONTENT route (quick lesson)

Reminder (§4): CONTENT never creates a path, so its lesson is in the response payload, not on screen.

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| D1 | Answered directly | Enter `Explain Python list comprehensions` → **Generate** | The dashboard stays put and shows the **"Answered directly"** notice explaining the query read as a single question rather than a learning path; no navigation, no crash | High |
| D3 | Lesson body generated | Read the `/sessions/stream` `done` frame (browser devtools network tab, or `curl -N -X POST localhost:8000/sessions/stream -H 'Content-Type: application/json' -d '{"user_id":"qa","query":"Explain Python list comprehensions","format_type":"B"}'`) | `route` is `CONTENT`, `session_id` is null, and `final_content` holds a single markdown lesson. Backend logs show the dual path: `Factual: gathered N result(s)` (or `Factual: no web results…`) then `Synthesizer: merging creative draft with factual findings` | High |

### Suite E — EXERCISE route (standalone)

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| E1 | Generate an exercise | Enter `Give me a coding exercise on Python loops` → **Generate**, then read the `done` frame as in D3 | The dashboard shows the **"Answered directly"** notice; the payload's `route` is `EXERCISE` and `exercise` carries a `statement` plus a `grading_artifact` (unit tests or rubric). Logs show `FormatSelector: chose …` | High |
| E3 | Generation failure is handled | If the LLM call fails (Groq exhausted and Ollama down/erroring) | The dashboard shows the API's message — *"The learning service is busy or rate-limited. Please try again in a moment."* — in the error colour, not a stack trace; clicking **Generate** again recovers | Low |

### Suite G — Resilience & negative cases

| ID | Title | Steps | Expected | Sev |
|---|---|---|---|---|
| G1 | Grade empty solution | Leave the editor empty (or whitespace-only) | **Grade my submission** is **disabled** (greyed) and cannot be clicked, so no grading runs (`web/components/LessonPanel.tsx`) | Medium |
| G2 | Missing function name | Submit code that doesn't define the required name | Graded as a failure (e.g. `0/0` / collect error), no crash | Medium |
| G3 | Infinite loop guard | Submit a coding solution with `while True: pass` | Grading **times out** at 10s (`DEFAULT_TIMEOUT_SECONDS`, `tools/code_executor.py`) and the result panel reports "Execution timed out (possible infinite loop)." — UI does not hang | High |
| G4 | Long session | Generate roadmap, open many nodes, grade several | No memory/state corruption; mastery and graph stay consistent | Medium |
| G5 | Factual arm degradation | (If web search errors in logs) | Lesson still generates from the creative draft; no hard failure | Low |

---

## 6. Cross-cutting checks

- **Logging:** terminal + `logs/mindmorph.log` should show structured lines for each stage, e.g.
  `service: astream_session (query=…)`, `service: composing lesson for node <id>`,
  `service: lesson <id> HIT (cache_hits=N)`, `service: grading <format> submission for node <id>`,
  `service: adapting graph after grade on node <id> (score=…)`,
  `Adaptation: +N node(s) +M edge(s)…`, `ContentAgent: generating … (remediation=True)`. LLM provider
  events use the `llm:` prefix — `llm: key N/M failed with HTTP 429 … rotating to next key` (WARNING),
  `llm: all N keys exhausted — falling back` (WARNING), `llm: key N/M failed permanently with HTTP 404
  — NOT rotating, falling back` (ERROR). No secrets (API keys) should ever appear in logs — key
  positions are logged as `key 2/3`, never the key value.
- **Security note (do not "fix" as a bug):** code grading runs the learner's Python in a subprocess
  with a wall-clock timeout and secret-scrubbed env. It is a **hang-guard for a local single-user
  prototype, not a security sandbox** (documented in `tools/code_executor.py`). Real isolation is a
  future hosted-deployment item. Do **not** run hostile code against a shared/production host.

---

## 7. Exit criteria

A build passes QA when:
- All **Critical** and **High** cases in Suites A–C pass.
- No unhandled stack traces surface in the UI for any case in Suites D–G.
- `pytest -q` is green.
- Known **out-of-scope** gaps (Section 2) are the only failures, logged as "expected / roadmap."

---

## Appendix A — Exercise solution recipes

Exercises are LLM-generated, so the **function name, signature, and unit tests differ every run**.
The reliable procedure:

1. Fetch the tests. They are **not** shown in the lesson panel — read
   `learning_session.lessons.<node_id>.exercise.grading_artifact.unit_tests` from
   `GET /sessions/<user>/<session>` (curl, or the devtools network tab on the lesson request). Read
   `from solution import <NAME>` and each `def test_…` assert.
2. Your editor content = the full `solution.py`. Define `<NAME>` to match.
3. Pick the outcome you want to test:

**CORRECT (→ 100% → ✓ Complete):** implement `<NAME>` so every assert passes. Watch for edge cases
the tests demand — e.g. raising `ValueError` on negative input, **with the exact message** the test
checks (`assert str(e) == '…'`).

**WRONG (→ 0% → `needs_review` + remediation lock, displays 🔒):** define the name but return a value
that fails every assert. Universal:
```python
def <NAME>(*args, **kwargs):
    return None
```

**PARTIAL (→ 40–79% → ◐ Keep practicing):** implement the core logic correctly but skip the
edge/validation cases, so some tests pass and some fail. Then **read `passed/total`** and tune:
- landed ≥ 80% → break one more case;
- landed < 40% → fix one case.

**Worked example.** For a harness like:
```python
from solution import calculate_rectangle_properties
def test_normal():            # (5,3) -> {'area':15,'perimeter':16}
def test_zero_length():       # (0,3) -> {'area':0,'perimeter':6}
def test_negative_length():   # (-5,3) -> raises ValueError('Length and width must be non-negative')
```
- Correct (100%):
  ```python
  def calculate_rectangle_properties(length, width):
      if length < 0 or width < 0:
          raise ValueError('Length and width must be non-negative')
      return {'area': length * width, 'perimeter': 2 * (length + width)}
  ```
- Partial (passes the positive/zero tests, fails the negative ones):
  ```python
  def calculate_rectangle_properties(length, width):
      return {'area': length * width, 'perimeter': 2 * (length + width)}
  ```
- Wrong (0%):
  ```python
  def calculate_rectangle_properties(length, width):
      return {}
  ```

> Tip: the worked-example numbers printed in the **statement** are sometimes themselves
> LLM-miscalculated. Trust the **unit tests in `grading_artifact`**, not the prose examples.

---

## Appendix B — Defect report template

```
Title:        [Suite-ID] short summary
Severity:     Critical | High | Medium | Low
Route:        SCOUT | CONTENT | EXERCISE
Build:        git commit hash (run `git rev-parse --short HEAD`)
Query used:   <the exact text typed>
Steps:        1) … 2) … 3) …
Expected:     …
Actual:       …
Evidence:     screenshot + relevant lines from terminal / logs/mindmorph.log
Notes:        (LLM output is non-deterministic — include the seed query so we can re-run)
```

---

## Appendix C — Quick reference

| Thing | Value |
|---|---|
| Launch | db: `docker compose up -d db`; backend: `conda run -n mindmorph uvicorn api.main:app --port 8000` (or prefix `MINDMORPH_STORE=memory` for no infra); frontend: `cd web && npm run dev` → http://localhost:3000 |
| Tests | `conda run -n mindmorph python -m pytest -q` |
| Logs | terminal + `logs/mindmorph.log` |
| Status glyphs | ✓ Complete (node + all prereqs mastered) · 🔒 Locked (prereqs pending or sub-40 remediation lock) · ◐ Keep practicing 40–79 · ↻ Needs review < 40 · ○ Available (mastery is sticky) |
| Routes | SCOUT = roadmap+loop (creates a path) · CONTENT = quick lesson · EXERCISE = standalone exercise (neither creates a path) |
| Leave a path | **← All paths** (back to dashboard) · **`<email>` · sign out** (back to the login gate; paths persist) |
| Required secret | **none** — `GROQ_API_KEYS` (pool) speeds things up; unset = local Ollama only |
| LLM backends | Groq `openai/gpt-oss-120b` (if keys) → local Ollama `qwen2.5:7b` / `:14b` fallback |
